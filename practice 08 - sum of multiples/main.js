

// Prompt the user to enter a multiple
let userMul = Number(prompt('Enter the multiple'));
let n = userMul;


// Prompt the user to enter a limit number
let userLimit = Number(prompt('Enter the multiple'));
let m = userLimit;


// Call the function sumMul (n, m)
let answer = sumMul (userMul, userLimit);


// Print the answer
document.write ('Your answer is: ' + answer);
  
  