// Prompt the user to enter an input
let userFirstInput = prompt('enter first input');
let first = userFirstInput;

let userSecondInput = prompt('enter first input');
let second = userSecondInput;

let userThirdInput = prompt('enter first input');
let third = userThirdInput;

// Call the function TripleTrouble(first, second, third)

let answer = TripleTrouble(userFirstInput, userSecondInput, userThirdInput);

// Print the answer
document.write (answer);